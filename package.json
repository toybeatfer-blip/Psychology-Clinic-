{
  "name": "psychology-clinic-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix backend run build && npm --prefix frontend install && npm --prefix frontend run build && node -e \"const fs=require('fs'); try { fs.cpSync('frontend/dist', 'dist', {recursive: true}); fs.cpSync('frontend/dist', 'backend/dist/public', {recursive: true}); } catch(e){}\"",
    "start": "node backend/dist/server.js",
    "dev": "npm --prefix backend run dev"
  }
}
